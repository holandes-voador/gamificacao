<p id="notice"><%= notice %></p>

<p>
  <strong>Item:</strong>
  <%= @reward.title %>
</p>

<p>
  <strong>Descrição:</strong>
  <%= @reward.description %>
</p>

<p>
  <strong>Quantidade de pontos para resgate:</strong>
  <%= @reward.cost%>
</p>

        <% if current_user.admin? %>
<%= link_to 'Edit', edit_reward_path(@reward) %> |
<% end %>
<%= link_to 'Voltar', rewards_path %>
